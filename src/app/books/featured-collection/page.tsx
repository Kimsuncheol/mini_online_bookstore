import { buildCategoryMetadata, buildCategoryPage } from '../categoryHelpers'

export const metadata = buildCategoryMetadata('featured-collection')

export default buildCategoryPage('featured-collection')
